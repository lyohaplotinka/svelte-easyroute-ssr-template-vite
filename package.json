{
  "name": "svelte-easyroute-ssr-template-vite",
  "version": "0.0.0",
  "scripts": {
    "dev": "vite",
    "build": "vite build -c vite.config.js --target esnext && vite build --ssr ./src/App.ssr.svelte -c vite.config.ssr.js --target es2018",
    "start": "vite preview",
    "start:ssr": "node ./server.js"
  },
  "devDependencies": {
    "@svitejs/vite-plugin-svelte": "^0.11.1",
    "express": "^4.17.1",
    "svelte": "^3.35.0",
    "vite": "^2.1.0"
  },
  "dependencies": {
    "svelte-easyroute": "^3.1.2"
  }
}
